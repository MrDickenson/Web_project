# 2. Шарова архітектура (Layered Architecture)

**Дата:** 03.12.2025
**Статус:** Прийнято

## Контекст
У зв'язку з вибором монолітної архітектури та планами розширення функціоналу, необхідно встановити чіткі правила організації коду. Без структури існує ризик змішування логіки обробки HTTP-запитів, бізнес-правил та роботи з базою даних, що ускладнить підтримку та тестування.

## Рішення
Ми впроваджуємо **тришарову архітектуру**. Код розподіляється по папках `api/`, `service/` та `domain/` з суворими правилами відповідальності.

### 1. Відповідальність шарів

* **`api/`**
    * **Що зберігається:** HTTP-контролери, маршрути (routes), DTO (Data Transfer Objects) для запитів/відповідей.
    * **Завдання:** Прийом запитів, валідація вхідних даних (формат), виклик методів сервісу, формування HTTP-відповіді (код статусу, тіло відповіді).
    * **Логіка:** Тільки логіка представлення (parsing, formatting).

* **`service/`**
    * **Що зберігається:** Класи/функції сервісів, реалізація бізнес-сценаріїв (Use Cases).
    * **Завдання:** Виконання бізнес-логіки, обробка транзакцій, оркестрація викликів до бази даних, складні перевірки.
    * **Логіка:** Основна логіка застосунку.

* **`domain/`**
    * **Що зберігається:** Сутності бази даних (Entities/Models), схеми таблиць, інтерфейси репозиторіїв (або прямі виклики ORM/DB драйвера).
    * **Завдання:** Представлення структури даних, безпосередня взаємодія з БД (SQL запити або ORM методи).

### 2. Напрямок залежностей
Залежності спрямовані строго **зверху вниз**:
`api/` -> `service/` -> `domain/`

### 3. Заборонені зв'язки (Правила)
* 🚫 **API не звертається до БД напряму.** Шар `api/` не повинен містити SQL-запитів або викликів методів репозиторіїв. Будь-який доступ до даних має проходити через `service/`.
* 🚫 **Бізнес-логіка не в API.** Контролери не повинні приймати бізнес-рішень (наприклад, "чи можна користувачу видалити це завдання"). Контролер лише транслює намір користувача сервісу.
* 🚫 **Циклічні залежності.** Сервіси не повинні залежати від контролерів.

## Наслідки
* **Тестування:** Бізнес-логіка в service/ покривається Unit-тестами незалежно від реалізації роботи з даними в domain/.
* **Підтримка:** Зміни в API (наприклад, перехід з REST на GraphQL) не зачеплять бізнес-логіку. Зміни в БД зачеплять `domain` і, можливо, `service`, але не `api`.
* **Boilerplate:** Для простих CRUD-операцій може з'явитися "проксі-код", коли метод сервісу просто викликає метод репозиторію без додаткової логіки. Це допустима ціна за чистоту архітектури.